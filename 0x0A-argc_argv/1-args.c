#include <stdio.h>
/**
 * main - a program that prints the number of arguments
 * @argc: a function that counts argument
 * @argv: a function that stores argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc);

		return (0);
}
